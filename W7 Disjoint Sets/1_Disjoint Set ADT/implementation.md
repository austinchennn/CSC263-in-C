## Disjoint Set（ADT）— Implementation（实现）

- Disjoint Set 本身只是一个 ADT，并没有唯一固定的实现方式——MakeSet / FindSet / Union 这三个操作可以用不同的数据结构来实现，各自的时间复杂度也不同
- 具体实现见后续小节
  - 链表实现，见 `2_Linked List Implementations/implementation.md`
  - 树实现，见 `3_Tree Implementations/implementation.md`
