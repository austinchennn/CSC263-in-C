## Topological Sort — 定义

- Topological Sort（拓扑排序）是对图 G 所有顶点的一个线性排序，使得如果 G 中存在边 (u, v)，那么 u 在排序中出现在 v 之前
- 如果图中含有环，则不存在这样的线性排序
- NOTE：拓扑排序是把图的顶点沿着一条水平线排列，使得所有有向边都从左指向右

### Theorem

- 对任意一对不同的顶点 u, v ∈ V，如果 G 中存在一条从 u 到 v 的边，那么 v.f < u.f
